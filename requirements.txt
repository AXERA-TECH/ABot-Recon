# ABot-Recon on Axera NPU — Python dependencies (verified with Python 3.12 on the AXCL host).
#
# NOT on pip: the Axera runtime (libaxcl_rt for PCIe cards, libax_engine/libax_sys on-chip) and the
# pyaxengine package (`axengine`) come from the Axera SDK. Optional: pyaxvideo (ax-video-sdk wheel,
# https://github.com/AXERA-TECH/ax-video-sdk/releases/tag/pyaxvideo-latest) for hardware video decode.
# No torch / torchvision / open3d / safetensors / onnxruntime: the host side (preprocessing, pose
# head, point clouds) is plain numpy, so the whole thing runs on a bare AX650 board too.

numpy>=1.26              # (2.4.6)
cffi>=1.16               # (2.1)        pyaxengine C bindings
pillow>=10               # (11.x)       image IO
opencv-python-headless>=4.10  # (4.13.0) frame extraction from video (service only)
matplotlib>=3.9          # (3.11.2)     floorplan / oblique / top-down renders (service only)
viser>=1.1               # (1.1.0)      web 3D canvas (service only)
fastapi>=0.110           # (0.136.3)    REST API + dashboard (service only)
uvicorn>=0.29            # (0.49.0)     ASGI server (service only)
python-multipart>=0.0.9  #             multipart video upload (service only)

# optional, validation scripts only:
#   onnxruntime                              scripts/validate_onnx.py   (ONNX golden chain)
#   torch torchvision open3d safetensors     scripts/validate_torchfree.py (parity vs the originals)
