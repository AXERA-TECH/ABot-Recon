# ABot-Recon on Axera NPU — Python dependencies (verified with Python 3.12 on the AXCL host).
#
# NOT on pip: the Axera runtime (libaxcl_rt for PCIe cards, libax_engine/libax_sys on-chip) and the
# pyaxengine package (`axengine`) come from the Axera SDK. abot_axera/native_runner.py only needs
# numpy + cffi + pyaxengine, so a bare AX650 board can run the NPU part without torch.
#
# Versions are floors; values in parentheses are what was verified.

torch>=2.4               # (2.10.0+cpu) CPU build — host pose head + assembly
torchvision>=0.19        # (0.25.0)     preprocessing: bicubic+antialias width-lock resize
numpy>=1.26              # (2.4.6)
cffi>=1.16               # (2.1)        pyaxengine C bindings
opencv-python>=4.10      # (4.13.0)     frame extraction from video
pillow>=10               # (11.x)       image IO
safetensors>=0.4         # (0.8.2)      AdjacentPoseHead weights
einops>=0.7              # (0.8.2)
matplotlib>=3.9          # (3.11.2)     floorplan / oblique / top-down renders
open3d>=0.18             # (0.19.0)     point-cloud PLY write + viser cloud load
viser>=1.1               # (1.1.0)      web 3D canvas
fastapi>=0.110           # (0.136.3)    REST API + dashboard
uvicorn>=0.29            # (0.49.0)     ASGI server
python-multipart>=0.0.9  #             multipart video upload
onnxruntime>=1.18        # (1.26.0)     optional: ONNX golden chain for scripts/validate_onnx.py
